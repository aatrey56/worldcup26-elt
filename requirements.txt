# Python deps for the Astro Runtime image (the Airflow environment).
# dbt itself is NOT here: it lives in an isolated venv built in the Dockerfile
# (dbt and Airflow can have conflicting deps), and Cosmos runs it from there.

# dbt-in-Airflow orchestration
astronomer-cosmos==1.14.2

# used by the extract/load task (include/extract/load_raw.py)
duckdb
requests
tenacity
