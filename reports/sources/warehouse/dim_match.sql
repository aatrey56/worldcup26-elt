select
  match_key,
  match_no,
  match_id,
  match_date,
  kickoff_et,
  stage,
  round_label,
  group_letter,
  home_team_key,
  away_team_key,
  venue_key,
  channel_en,
  channel_es
from main.dim_match
